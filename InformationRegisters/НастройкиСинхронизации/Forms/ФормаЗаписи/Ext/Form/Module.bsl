// Не дает редактировать содержимое пользователю в мобильном клиенте&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если ПараметрыСеанса.ЭтоМобильныйКлиент Тогда 
        Элементы.Ключ.ТолькоПросмотр = Истина;
		Элементы.ИзменятьСоставСтрок = Ложь;
	Иначе 
		Сообщить("Это сервер");
	КонецЕсли;
		
КонецПроцедуры