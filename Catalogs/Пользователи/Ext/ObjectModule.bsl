
Процедура ПередЗаписью(Отказ)
    #Если МобильноеПриложениеСервер Тогда
        Если Не ЭтоНовый() Тогда
            // Запрещаем редактирование существующих элементов
            Отказ = Истина;
            ВызватьИсключение НСтр("ru='Редактирование справочника запрещено в мобильном клиенте!'");
        КонецЕсли;
    #КонецЕсли
КонецПроцедуры