// Если пользователь нее авторизован, то он не имеет доступ к справочникам и документам
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Если СервисАвторизации.ПолучениеПараметраСеансаАдминистратора() Тогда 
		// Вывод сообщения, что доступ разрешен	
		Сообщить("Доступ разрешен");
			
	Иначе 
		// Вывод исключение не позволяющее просмотреть нанные в справочниках и документах	
		ВызватьИсключение "Эта форма не доступна";
			
	КонецЕсли;
	
КонецПроцедуры
